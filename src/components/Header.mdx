---
name: Header
menu: Components
---

import { Props, Playground } from 'docz';
import { Header } from './Header';

# Header Component

The Header component is used to display the current page title.

<Props of={Header} />

## Basic Usage

The title text is handled by setting the `title` property. THe default value is "Enter Title Here".

<Playground>
  <Header text="I am a Title" />
</Playground>
